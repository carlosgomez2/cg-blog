section#articles.articles
  .flex.flex--center
    .col-50.flex.flex--center
      .col-1
        .desc-wrap
          p.home-lead.outdented ...Trying to not forget how things work...
          a(target="_blank" href="/records").home-cta FOR THE RECORD

    .col-50
      .article-wrap
        | {% for post in site.posts limit: 12 %}
        a(href="{{post.url}}" style="background-image: url({{post.cover}})").article-thumb
          .article-meta
            .name {{post.title}}
            .date {{post.date | date: "%b %d, %Y"}}
        | {% endfor %}
